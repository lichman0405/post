{
  "name": "@post/domain-fixtures",
  "version": "0.1.0",
  "private": true,
  "description": "Shared, versioned test fixtures for POST domain objects. Fixtures are derived from specs/schemas/ and the examples/ seed documents; the fixture set arrives with the test tasks (T0002 scaffolds the package only)."
}
