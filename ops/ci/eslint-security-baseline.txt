# eslint-plugin-security baseline (T1209) — the Node/TypeScript findings that
# predate the SAST row.
#
# One line per finding:   path:line:column:RULE<TAB>category<TAB>reason
#
# Read and enforced by tests/security/sast_report.py, which refuses a key that
# names a directory or a glob: the entries below are findings, each judged on
# its own line.
#
# NOTHING HERE IS AN ACCEPTED RISK. docs/23 §11 routes an accepted High to a
# written ADR, which is the Supervisor's document; this file records only the
# findings a reviewer judged not to be a security issue.
#
# tool: eslint 9.39.5 with eslint-plugin-security 4.0.1, its whole
#       recommended rule set, every rule forced to "error" (the config file
#       tests/security/node-tools/eslint-sast.config.mjs is the rule surface).
# surface: apps/web and packages/ui: 129 files, 88 findings.
# review: 78 of the 88 are security/detect-object-injection, which fires on
#       every computed property access; each of those lines is quoted below
#       with the key that reaches it. The remaining ten are the plugin's
#       regex, fs and timing rules over the app's own source.

apps/web/app/(main)/explore/explore-tabs.tsx:97:5:security/detect-object-injection	not-a-security-issue	tabRefs.current[next]?.focus();: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/app/(main)/explore/explore-tabs.tsx:134:24:security/detect-object-injection	not-a-security-issue	const Icon = TAB_ICONS[tab];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/app/(main)/explore/explore-tabs.tsx:148:17:security/detect-object-injection	not-a-security-issue	tabRefs.current[tab] = el;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/app/(main)/page.tsx:60:26:security/detect-object-injection	not-a-security-issue	.map((n) => `${n}: ${checks[n]?.status ?? "?"}`): the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/app/(main)/projects/[id]/conflicts/page.tsx:686:25:security/detect-object-injection	not-a-security-issue	return JSON.stringify((payload as Record<string, unknown>)[key], null, 2);: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/app/(main)/projects/[id]/files/page.tsx:524:52:security/detect-object-injection	not-a-security-issue	return `${value.toFixed(value >= 100 ? 0 : 1)} ${units[unit]}`;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/app/(main)/projects/[id]/pulls/[number]/page.tsx:131:20:security/detect-object-injection	not-a-security-issue	const target = PULL_TABS[next];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/app/(main)/projects/project-tabs.ts:75:29:security/detect-object-injection	not-a-security-issue	return role !== null && ROLE_RANK[role] >= ROLE_RANK[tab.minRole];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/app/components/research-profile-sections.tsx:104:19:security/detect-object-injection	not-a-security-issue	const a = items[i];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/app/components/research-profile-sections.tsx:148:19:security/detect-object-injection	not-a-security-issue	const c = items[i];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/app/components/research-profile-sections.tsx:189:19:security/detect-object-injection	not-a-security-issue	const a = items[i];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/app/components/research-profile-sections.tsx:226:19:security/detect-object-injection	not-a-security-issue	const r = items[i];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/app/components/research-profile-sections.tsx:255:19:security/detect-object-injection	not-a-security-issue	const r = items[i];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/app/nav/language-switcher.tsx:69:33:security/detect-object-injection	not-a-security-issue	{translate(candidate, LOCALE_LABEL_KEY[candidate])}: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/activity.ts:437:10:security/detect-object-injection	not-a-security-issue	return VIA_NAMES[via] ?? via;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/activity.ts:458:13:security/detect-object-injection	not-a-security-issue	const v = facts[key];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/activity.ts:509:7:security/detect-possible-timing-attacks	not-a-security-issue	if (hash !== "") out.push({ label: "Manifest", value: shortHash(hash) });: this compares a non-secret value (an activity payload's own fact string) against the empty string; there is no secret on either side
apps/web/lib/assets.test.mjs:58:21:security/detect-object-injection	not-a-security-issue	const handler = routes[path];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/assets.ts:68:10:security/detect-object-injection	not-a-security-issue	return (TYPE_LABEL_KEYS as Record<string, string>)[type] ?? null;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/assets.ts:88:10:security/detect-object-injection	not-a-security-issue	return (TYPE_EMPTY_KEYS as Record<string, string>)[type] ?? null;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/attestations.ts:145:10:security/detect-object-injection	not-a-security-issue	return TYPE_LABELS[type] ?? type;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/attestations.ts:150:10:security/detect-object-injection	not-a-security-issue	return RESULT_LABELS[result] ?? result;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/auth.ts:140:24:security/detect-object-injection	not-a-security-issue	if (csrf !== null) headers[CSRF_HEADER] = csrf;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/config.ts:177:15:security/detect-object-injection	not-a-security-issue	const raw = env[key] ?? "";: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/discussions.ts:356:24:security/detect-object-injection	not-a-security-issue	if (csrf !== null) headers[CSRF_HEADER] = csrf;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/entity-meta.ts:255:10:security/detect-unsafe-regex	not-a-security-issue	return /^[A-Za-z0-9.\-]+(:\d{1,5})?$/.test(host) || /^\[[0-9A-Fa-f:.]+\](:\d{1,5})?$/.test(host);: the pattern has no nested quantifier over the same characters and is anchored, so there is no catastrophic backtracking; the rule fires on the quantifier shape alone
apps/web/lib/entity-meta.ts:255:55:security/detect-unsafe-regex	not-a-security-issue	return /^[A-Za-z0-9.\-]+(:\d{1,5})?$/.test(host) || /^\[[0-9A-Fa-f:.]+\](:\d{1,5})?$/.test(host);: the pattern has no nested quantifier over the same characters and is anchored, so there is no catastrophic backtracking; the rule fires on the quantifier shape alone
apps/web/lib/entity-meta.ts:270:17:security/detect-object-injection	not-a-security-issue	const value = body[key];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/explore.test.mjs:47:21:security/detect-object-injection	not-a-security-issue	const handler = routes[path];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/explore.test.mjs:59:35:security/detect-object-injection	not-a-security-issue	for (const tab of EXPLORE_TABS) body[tab] = { tab, items: [] };: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/explore.test.mjs:66:3:security/detect-object-injection	not-a-security-issue	body[tab] = { tab, items: [item] };: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/explore.test.mjs:153:12:security/detect-object-injection	not-a-security-issue	delete body[tab];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/explore.ts:65:10:security/detect-object-injection	not-a-security-issue	return (TAB_LABELS as Record<string, string>)[tab] ?? tab;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/explore.ts:213:19:security/detect-object-injection	not-a-security-issue	const section = sections[tab];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/explore.ts:231:21:security/detect-object-injection	not-a-security-issue	const section = record[tab];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/i18n.test.mjs:96:47:security/detect-object-injection	not-a-security-issue	for (const [key, value] of Object.entries(MESSAGES[locale])) {: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/i18n.test.mjs:108:23:security/detect-unsafe-regex	not-a-security-issue	assert.match(key, /^[a-z][A-Za-z0-9-]*(\.[A-Za-z0-9-]+)+$/, `key shape: ${key}`);: the pattern has no nested quantifier over the same characters and is anchored, so there is no catastrophic backtracking; the rule fires on the quantifier shape alone
apps/web/lib/i18n.test.mjs:143:47:security/detect-object-injection	not-a-security-issue	for (const [key, value] of Object.entries(MESSAGES[locale])) {: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/i18n.test.mjs:196:9:security/detect-object-injection	not-a-security-issue	MESSAGES.en[key].includes(token),: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/i18n.test.mjs:197:52:security/detect-object-injection	not-a-security-issue	`en:${key} lost ${token}: ${JSON.stringify(MESSAGES.en[key])}`,: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/i18n.test.mjs:200:9:security/detect-object-injection	not-a-security-issue	MESSAGES["zh-CN"][key].includes(token),: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/i18n.test.mjs:201:55:security/detect-object-injection	not-a-security-issue	`zh-CN:${key} lost ${token}: ${JSON.stringify(MESSAGES["zh-CN"][key])}`,: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/i18n.ts:1265:17:security/detect-object-injection	not-a-security-issue	const table = MESSAGES[locale] ?? MESSAGES[DEFAULT_LOCALE];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/i18n.ts:1265:37:security/detect-object-injection	not-a-security-issue	const table = MESSAGES[locale] ?? MESSAGES[DEFAULT_LOCALE];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/i18n.ts:1266:17:security/detect-object-injection	not-a-security-issue	const value = table[key];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/i18n.ts:1272:25:security/detect-object-injection	not-a-security-issue	const replacement = vars[name];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/inbox.ts:171:24:security/detect-object-injection	not-a-security-issue	if (csrf !== null) headers[CSRF_HEADER] = csrf;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/inbox.ts:318:10:security/detect-object-injection	not-a-security-issue	return EVENT_TYPE_NAMES[eventType] ?? eventType;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/milestones.ts:198:24:security/detect-object-injection	not-a-security-issue	if (csrf !== null) headers[CSRF_HEADER] = csrf;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/milestones.ts:199:39:security/detect-object-injection	not-a-security-issue	if (idempotencyKey !== undefined) headers[IDEMPOTENCY_HEADER] = idempotencyKey;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/projects.ts:159:24:security/detect-object-injection	not-a-security-issue	if (csrf !== null) headers[CSRF_HEADER] = csrf;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/publish.ts:222:24:security/detect-object-injection	not-a-security-issue	if (csrf !== null) headers[CSRF_HEADER] = csrf;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/publish.ts:223:39:security/detect-object-injection	not-a-security-issue	if (idempotencyKey !== undefined) headers[IDEMPOTENCY_HEADER] = idempotencyKey;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/publish.ts:307:15:security/detect-object-injection	not-a-security-issue	const a = bytes[i];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/publish.ts:325:32:security/detect-object-injection	not-a-security-issue	const enc1 = chars.indexOf(base64[i]);: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/publish.ts:338:5:security/detect-object-injection	not-a-security-issue	bytes[i] = binary.charCodeAt(i);: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/publish.ts:394:22:security/detect-object-injection	not-a-security-issue	.filter((k) => record[k] !== undefined): the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/publish.ts:395:58:security/detect-object-injection	not-a-security-issue	.map((k) => `${JSON.stringify(k)}:${canonicalValue(record[k])}`);: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/publish.ts:414:55:security/detect-object-injection	not-a-security-issue	(key) => `${JSON.stringify(key)}:${canonicalValue(manifest.metadata[key])}`,: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/pulls.test.mjs:105:19:security/detect-object-injection	not-a-security-issue	const route = routes[key];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/pulls.test.mjs:247:12:security/detect-object-injection	not-a-security-issue	delete missing[field];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/pulls.ts:437:18:security/detect-object-injection	not-a-security-issue	if (typeof source[field] !== "string") {: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/pulls.ts:469:18:security/detect-object-injection	not-a-security-issue	if (typeof source[field] !== "string") {: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/pulls.ts:510:18:security/detect-object-injection	not-a-security-issue	if (typeof summary[field] !== "number") {: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/releases.ts:132:24:security/detect-object-injection	not-a-security-issue	if (csrf !== null) headers[CSRF_HEADER] = csrf;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/releases.ts:133:39:security/detect-object-injection	not-a-security-issue	if (idempotencyKey !== undefined) headers[IDEMPOTENCY_HEADER] = idempotencyKey;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/research-profile.test.mjs:44:21:security/detect-object-injection	not-a-security-issue	const handler = routes[path];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/research-profile.test.mjs:240:5:security/detect-object-injection	not-a-security-issue	dirty.person[key] = 1;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/research-profile.test.mjs:243:7:security/detect-non-literal-regexp	not-attacker-reachable	new RegExp(`\\.${key}`), builds a RegExp from a literal held in the file's own table; no request contributes to the pattern
apps/web/lib/research-profile.ts:227:17:security/detect-object-injection	not-a-security-issue	const value = source[key];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/research-profile.ts:236:17:security/detect-object-injection	not-a-security-issue	const value = source[key];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/rights-copy.test.mjs:122:25:security/detect-non-literal-fs-filename	not-attacker-reachable	for (const entry of readdirSync(dir, { withFileTypes: true })) { reads a file under the test's own __dirname or a directory the build wrote; no request names it
apps/web/lib/rights-copy.test.mjs:137:10:security/detect-non-literal-fs-filename	not-attacker-reachable	return readFileSync(join(WEB_ROOT, relativePath), "utf8"); reads a file under the test's own __dirname or a directory the build wrote; no request names it
apps/web/lib/rights-copy.test.mjs:259:26:security/detect-non-literal-regexp	not-attacker-reachable	assert.match(sample, new RegExp(source, "i"), `no match, so nothing is refused: ${sample}`); builds a RegExp from a literal held in the file's own table; no request contributes to the pattern
apps/web/lib/rights-copy.test.mjs:311:13:security/detect-non-literal-fs-filename	not-attacker-reachable	assert.ok(statSync(join(WEB_ROOT, "lib", "rights.ts")).isFile()); reads a file under the test's own __dirname or a directory the build wrote; no request names it
apps/web/lib/rights.test.mjs:99:18:security/detect-non-literal-fs-filename	not-attacker-reachable	const source = readFileSync( reads a file under the test's own __dirname or a directory the build wrote; no request names it
apps/web/lib/rights.ts:192:22:security/detect-object-injection	not-a-security-issue	const declared = usageSource[axis];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/rights.ts:194:7:security/detect-object-injection	not-a-security-issue	usage[axis] = declared;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/rights.ts:196:7:security/detect-object-injection	not-a-security-issue	usage[axis] = NOT_STATED;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/rights.ts:203:22:security/detect-object-injection	not-a-security-issue	const declared = visibilitySource[axis];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/rights.ts:205:7:security/detect-object-injection	not-a-security-issue	visibility[axis] = declared;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/rights.ts:207:7:security/detect-object-injection	not-a-security-issue	visibility[axis] = NOT_STATED;: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/rights.ts:257:10:security/detect-object-injection	not-a-security-issue	return labels[value] ?? verbatim(value);: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/rights.ts:348:14:security/detect-object-injection	not-a-security-issue	heading: AXIS_HEADINGS[axis],: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/rights.ts:349:15:security/detect-object-injection	not-a-security-issue	declared: document.usage[axis],: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
apps/web/lib/rights.ts:350:32:security/detect-object-injection	not-a-security-issue	rendered: usageLabel(axis, document.usage[axis]),: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
packages/ui/src/DevStatus.tsx:34:16:security/detect-object-injection	not-a-security-issue	const Icon = iconByState[state];: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
packages/ui/src/DevStatus.tsx:36:21:security/detect-object-injection	not-a-security-issue	<Label variant={variantByState[state]}>: the rule fires on every computed property access; the key at this line is a literal, a loop index, or a value narrowed to a closed union, so no path lets an attacker name __proto__ or constructor
