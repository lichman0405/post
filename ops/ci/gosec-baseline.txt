# gosec baseline (T1209) — the Go findings that predate the SAST row.
#
# One line per finding:   path:line:column:RULE<TAB>category<TAB>reason
#
# Read and enforced by tests/security/sast_report.py, which refuses a key that
# names a directory or a glob: the entries below are findings, each judged on
# its own line, not a class of finding.
#
# NOTHING HERE IS AN ACCEPTED RISK. docs/23 §11 routes an accepted High to a
# written ADR, which is the Supervisor's document; this file records only the
# findings a reviewer judged not to be a security issue, and the reason column
# says which line was judged and why.
#
# tool: gosec v2.29.0 over this module's own packages: 783 files, 256 findings
#       (53 HIGH, 161 MEDIUM, 42 LOW, as the row's severity-mix line reports).
#       Both numbers are re-derived, on 2026-09-24, from the row's own output:
#       `bash tests/security/sast.sh go` prints "783 file(s) scanned, 256
#       finding(s) reported" and the mix above, and the entries in THIS file —
#       which are the same 256 — by `grep -c '^[^#]' ops/ci/gosec-baseline.txt`.
#       They are what this tree printed on that date, not a list this comment
#       maintains: the file count follows the module, the finding count the
#       code. (Written 2026-09-23 as 797 files / 254 findings; the file count
#       moved when the row stopped walking the filesystem from the root and
#       started scanning the package set `go list ./...` reports — see the next
#       paragraph — and the finding count moved with two entries T1217 added.)
# surface: this module's own packages, as `go list ./...` names them. The row
#       derives that list, drops the package directories git ignores (installed
#       and generated state: with the pinned tools installed, an npm package
#       under tests/security/node-tools/node_modules/ ships a Go file the
#       toolchain returns as a package of this module), prints every directory
#       it dropped with the rule that dropped it, and checks the files of the
#       packages that remain before the scanner runs: each one under the
#       repository root, and not git-ignored (tests/security/sast.sh,
#       repo_paths_only). Nothing is excluded by directory, severity or -nosec:
#       the row passes no such flag — there is no -exclude-dir, no -nosec and no
#       severity floor in its invocation — and sast_report.py rejects any key
#       that is not file:line:rule. What the package set leaves out is what Go
#       does not call source: directories named testdata or beginning with `.`
#       or `_` (a fixture of tests/contract/testdata is data, not a package).
#       "Every package in the module" used to be the claim here, and it was not
#       true: the row walked the filesystem from the repository root instead, so
#       a working copy's .rddev/ — rebaseline drafts and other Workers'
#       worktrees, copies of this tree's source and of source nobody had
#       committed — was scanned and its findings reported as this tree's. CI
#       never saw it (a checkout has no .rddev/); every local gate run did.
#       Rewritten 2026-09-24 (T1220) with the surface it now describes.
# review: each entry quotes the flagged source line and names the construct it
#       belongs to. Gosec's own G115/G304/G703 templates cover most of the
#       volume; the sites where the honest verdict differs carry a note that
#       cites the validating code (a MaxInt32 check, a clamp, an int4 column).
#
# The 53 findings gosec calls HIGH are dominated by four rules: G304 (65: a
# non-literal path, in dev tooling and test harnesses), G204 (28: a constant
# binary with argv), G115 (22: int -> int32 narrowing) and G101 (12: the
# credential-entropy regex, which in this tree matches an error code, an
# environment variable's NAME, a help-text URL and the local dev database's
# published default password).

cmd/api/authhttp/auth_middleware.go:330:21:G124	not-a-security-issue	return &http.Cookie{: the cookie literal here sets HttpOnly and SameSite and binds Secure to a parameter — the rule's claim that a protection attribute is missing is wrong for this line
cmd/api/authhttp/auth_middleware.go:354:21:G124	not-a-security-issue	return &http.Cookie{: the cookie literal here sets HttpOnly and SameSite and binds Secure to a parameter — the rule's claim that a protection attribute is missing is wrong for this line
cmd/api/backupdr/drill.go:101:9:G301	not-attacker-reachable	return os.MkdirAll(abs, 0o755) creates a directory with mode 0755 inside the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url — the mode is the ordinary directory mode, and the path is not chosen by a request
cmd/api/backupdr/drill.go:378:20:G304	not-attacker-reachable	schemaRaw, err := os.ReadFile(schemaFile) reads a path built from the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
cmd/api/backupdr/drill.go:395:17:G304	not-attacker-reachable	if seq, err := os.ReadFile(filepath.Join(dir, filepath.FromSlash(fileSequences))); err == nil { reads a path built from the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
cmd/api/backupdr/drill.go:413:12:G301	not-attacker-reachable	if err := os.MkdirAll(blobDir, 0o755); err != nil { creates a directory with mode 0755 inside the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url — the mode is the ordinary directory mode, and the path is not chosen by a request
cmd/api/backupdr/drill.go:424:13:G306	not-attacker-reachable	if err := os.WriteFile(filepath.Join(blobDir, sum), body, 0o644); err != nil { writes with mode 0644/0755 inside the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url; the content is a generated artifact or a report, and the path is not chosen by a request
cmd/api/backupdr/drill.go:518:14:G304	not-attacker-reachable	raw, err := os.ReadFile(filepath.Join(backupDir, fileManifest)) reads a path built from the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
cmd/api/backupdr/drill.go:595:16:G304	not-attacker-reachable	body, err := os.ReadFile(filepath.Join(backupDir, filepath.FromSlash(b.File))) reads a path built from the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
cmd/api/backupdr/drill.go:797:12:G301	not-attacker-reachable	if err := os.MkdirAll(filepath.Dir(path), 0o755); err != nil { creates a directory with mode 0755 inside the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url — the mode is the ordinary directory mode, and the path is not chosen by a request
cmd/api/backupdr/drill.go:800:9:G306	not-attacker-reachable	return os.WriteFile(path, append(raw, '\n'), 0o644) writes with mode 0644/0755 inside the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url; the content is a generated artifact or a report, and the path is not chosen by a request
cmd/api/backupdr/git.go:89:9:G204	not-attacker-reachable	cmd := exec.CommandContext(ctx, bin, args...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url
cmd/api/backupdr/git.go:296:12:G301	not-attacker-reachable	if err := os.MkdirAll(filepath.Dir(dest), 0o755); err != nil { creates a directory with mode 0755 inside the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url — the mode is the ordinary directory mode, and the path is not chosen by a request
cmd/api/backupdr/git.go:472:2:G104	not-a-security-issue	the unchecked error at io.Copy(io.Discard, resp.Body) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url)
cmd/api/backupdr/postgres.go:137:9:G204	not-attacker-reachable	cmd := exec.CommandContext(ctx, bin, args[1:]...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url
cmd/api/backupdr/postgres.go:170:12:G301	not-attacker-reachable	if err := os.MkdirAll(filepath.Dir(dest), 0o755); err != nil { creates a directory with mode 0755 inside the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url — the mode is the ordinary directory mode, and the path is not chosen by a request
cmd/api/backupdr/postgres.go:173:9:G306	not-attacker-reachable	return os.WriteFile(dest, raw, 0o644) writes with mode 0644/0755 inside the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url; the content is a generated artifact or a report, and the path is not chosen by a request
cmd/api/backupdr/postgres.go:186:12:G301	not-attacker-reachable	if err := os.MkdirAll(dir, 0o755); err != nil { creates a directory with mode 0755 inside the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url — the mode is the ordinary directory mode, and the path is not chosen by a request
cmd/api/backupdr/postgres.go:196:13:G306	not-attacker-reachable	if err := os.WriteFile(filepath.Join(dir, "_sequences.sql"), sequences, 0o644); err != nil { writes with mode 0644/0755 inside the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url; the content is a generated artifact or a report, and the path is not chosen by a request
cmd/api/backupdr/postgres.go:214:13:G306	not-attacker-reachable	if err := os.WriteFile(file, b.render(), 0o644); err != nil { writes with mode 0644/0755 inside the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url; the content is a generated artifact or a report, and the path is not chosen by a request
cmd/api/backupdr/postgres.go:362:14:G304	not-attacker-reachable	raw, err := os.ReadFile(schemaFile) reads a path built from the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
cmd/api/backupdr/postgres.go:401:15:G304	not-attacker-reachable	raw, err := os.ReadFile(filepath.Join(dataDir, name)) reads a path built from the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
cmd/api/backupdr/postgres.go:414:17:G304	not-attacker-reachable	if seq, err := os.ReadFile(filepath.Join(dataDir, "_sequences.sql")); err == nil { reads a path built from the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
cmd/api/backupdr/run.go:256:14:G304	not-attacker-reachable	raw, err := os.ReadFile(path) reads a path built from the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
cmd/api/backupdr/s3.go:253:2:G104	not-a-security-issue	the unchecked error at resp.Body.Close() is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url)
cmd/api/backupdr/s3.go:274:2:G104	not-a-security-issue	the unchecked error at io.Copy(io.Discard, resp.Body) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url)
cmd/api/backupdr/secrets.go:161:26:G122	not-attacker-reachable	raw, err := os.ReadFile(path) runs inside a Walk callback over the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url; the symlink race the rule describes needs an attacker who can already write into that directory, which is the same trust domain as the process doing the walk
cmd/api/backupdr/secrets.go:161:15:G304	not-attacker-reachable	raw, err := os.ReadFile(path) reads a path built from the backup/restore drill, an operator-run tool whose input is the operator's own --dir/--url — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
cmd/api/feedshttp/handlers.go:152:16:G705	escaping-is-elsewhere	_, _ = w.Write(doc) writes an already-rendered document whose media type the handler declared and whose escaping happened in the renderer — this line is the transport, not the encoder
cmd/api/gittokenshttp/git_tokens.go:28:2:G101	not-a-security-issue	codeTokenNotFound = "GIT_TOKEN_NOT_FOUND": this is an API error code returned when a git token is not found; the token itself is never a literal
cmd/api/releasehttp/release_handlers.go:258:16:G705	escaping-is-elsewhere	_, _ = w.Write(doc) writes an already-rendered document whose media type the handler declared and whose escaping happened in the renderer — this line is the transport, not the encoder
cmd/rddev/db.go:15:7:G101	not-a-security-issue	the match is inside dbUsage, the command's own help text, which documents the same local default URL; a string in a help message is not a credential
cmd/rddev/db.go:63:3:G101	not-a-security-issue	dbURL = "postgres://postgres:postgres_dev_pw@127.0.0.1:5432/post": the literal is the local docker-compose stack's published default (POSTGRES_PASSWORD:-postgres_dev_pw, docker-compose.yml:31) on 127.0.0.1:5432 — it grants nothing outside a developer's own machine, and this copy adds no exposure the committed compose file does not already have
cmd/rddev/drive.go:344:19:G703	not-attacker-reachable	_ = os.WriteFile(reason, []byte(rebaselineReason(res)), 0o600) reads or writes a path taken from the rddev CLI, whose input is the operator's own command line; the taint source the rule needs is an attacker who can already choose that path (see the notes on the individual sites)
cmd/rddev/env.go:28:9:G204	not-attacker-reachable	cmd := exec.Command("docker", append([]string{"compose"}, args...)...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the rddev CLI, whose input is the operator's own command line
cmd/rddev/env.go:77:3:G104	not-a-security-issue	the unchecked error at enc.Encode(map[string]any{ is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev CLI, whose input is the operator's own command line)
cmd/rddev/gate.go:104:4:G104	not-a-security-issue	the unchecked error at enc.Encode(res) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev CLI, whose input is the operator's own command line)
cmd/rddev/gate.go:147:4:G104	not-a-security-issue	the unchecked error at enc.Encode(out) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev CLI, whose input is the operator's own command line)
cmd/rddev/gate.go:177:4:G104	not-a-security-issue	the unchecked error at enc.Encode(struct { is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev CLI, whose input is the operator's own command line)
cmd/rddev/gate.go:198:3:G104	not-a-security-issue	the unchecked error at enc.Encode(spec) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev CLI, whose input is the operator's own command line)
cmd/rddev/git.go:104:3:G104	not-a-security-issue	the unchecked error at enc.Encode(res) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev CLI, whose input is the operator's own command line)
cmd/rddev/pr.go:113:3:G104	not-a-security-issue	the unchecked error at enc.Encode(res) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev CLI, whose input is the operator's own command line)
cmd/rddev/pr.go:130:3:G104	not-a-security-issue	the unchecked error at enc.Encode(g4) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev CLI, whose input is the operator's own command line)
cmd/rddev/review.go:124:4:G104	not-a-security-issue	the unchecked error at enc.Encode(res) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev CLI, whose input is the operator's own command line)
cmd/rddev/review.go:146:4:G104	not-a-security-issue	the unchecked error at enc.Encode(report) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev CLI, whose input is the operator's own command line)
cmd/rddev/task.go:131:18:G304	not-attacker-reachable	data, err := os.ReadFile(rf) reads a path built from the rddev CLI, whose input is the operator's own command line — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
cmd/rddev/task.go:131:29:G703	not-attacker-reachable	data, err := os.ReadFile(rf) reads or writes a path taken from the rddev CLI, whose input is the operator's own command line; the taint source the rule needs is an attacker who can already choose that path (see the notes on the individual sites)
cmd/rddev/task.go:451:2:G104	not-a-security-issue	the unchecked error at enc.Encode(v) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev CLI, whose input is the operator's own command line)
cmd/rddev/worker.go:255:3:G104	not-a-security-issue	the unchecked error at enc.Encode(res) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev CLI, whose input is the operator's own command line)
cmd/rddev/worker.go:301:3:G104	not-a-security-issue	the unchecked error at enc.Encode(struct { is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev CLI, whose input is the operator's own command line)
cmd/rddev/worker.go:344:26:G703	not-attacker-reachable	data, err := os.ReadFile(rec.LogPath) reads or writes a path taken from the rddev CLI, whose input is the operator's own command line; the taint source the rule needs is an attacker who can already choose that path (see the notes on the individual sites)
cmd/rddev/worker.go:394:4:G104	not-a-security-issue	the unchecked error at enc.Encode(struct { is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev CLI, whose input is the operator's own command line)
cmd/rddev/worker_collect.go:32:4:G104	not-a-security-issue	the unchecked error at enc.Encode(report) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev CLI, whose input is the operator's own command line)
cmd/rddev/workflow.go:64:3:G104	not-a-security-issue	the unchecked error at enc.Encode(view) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev CLI, whose input is the operator's own command line)
internal/application/authn/config.go:57:2:G101	not-a-security-issue	DefaultOIDCRedirectPath = "/api/v1/auth/oidc/callback": this is a URL path (the OIDC callback route), not a credential; gosec's entropy check matched a path segment
internal/application/authn/errors.go:9:2:G101	not-a-security-issue	CodeInvalidCredentials = "AUTH_INVALID_CREDENTIALS": this is an API error code for a failed login, deliberately one code for both wrong-user and wrong-password
internal/application/authn/oidctest/provider.go:188:15:G710	not-attacker-reachable	http.Redirect(w, r, u.String(), http.StatusFound) is the OIDC test double (internal/application/authn/oidctest), which is only ever linked into tests
internal/application/authn/password.go:53:106:G115	bounded	got := argon2.IDKey([]byte(password), salt, params.iterations, params.memory, params.parallelism, ui: the conversion is uint32(len(key)) where key is the hash just produced by argon2.IDKey at the fixed argonKeyLen length
internal/application/templates/catalog.go:65:33:G602	bounded	raw, err := json.Marshal(pairs[i+1]): i is the even loop index over pairs, and the loop guard keeps i+1 inside the slice, so the index cannot be out of range
internal/config/cwd.go:28:23:G703	not-attacker-reachable	if st, err := os.Stat(candidate); err == nil && !st.IsDir() { reads or writes a path taken from the configuration loader, whose path comes from the process's own startup arguments; the taint source the rule needs is an attacker who can already choose that path (see the notes on the individual sites)
internal/config/envfile.go:28:15:G304	not-attacker-reachable	data, err := os.ReadFile(path) reads a path built from the configuration loader, whose path comes from the process's own startup arguments — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/config/envfile.go:28:26:G703	not-attacker-reachable	data, err := os.ReadFile(path) reads or writes a path taken from the configuration loader, whose path comes from the process's own startup arguments; the taint source the rule needs is an attacker who can already choose that path (see the notes on the individual sites)
internal/config/secretscan.go:152:27:G122	not-a-security-issue	data, err := os.ReadFile(path): the scanner is meant to read whatever the tree contains, including a path that a concurrent writer could swap for a symlink; nothing downstream trusts the file's identity for a security decision, so the race the rule describes has no consequence here
internal/config/secretscan.go:152:16:G304	not-a-security-issue	data, err := os.ReadFile(path): reading the file it walked to is the secret scanner's whole job; the bytes are only ever pattern-matched and reported, never returned to a caller or executed, so the file-inclusion sink the rule names does not exist at this line
internal/devorchestrator/dag.go:44:15:G304	not-attacker-reachable	data, err := os.ReadFile(path) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/doctor/eval.go:482:15:G304	not-attacker-reachable	data, err := os.ReadFile(pkg) reads a path built from the doctor, a developer-run diagnostic over the local stack — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/doctor/measure.go:33:15:G304	not-attacker-reachable	data, err := os.ReadFile(filepath.Join(o.FixtureDir, name)) reads a path built from the doctor, a developer-run diagnostic over the local stack — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/doctor/measure.go:47:15:G304	not-attacker-reachable	data, err := os.ReadFile(filepath.Join(o.FixtureDir, name)) reads a path built from the doctor, a developer-run diagnostic over the local stack — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/doctor/measure.go:176:14:G204	not-attacker-reachable	out, err := exec.Command("uname", flag).Output() runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the doctor, a developer-run diagnostic over the local stack
internal/devorchestrator/doctor/measure.go:227:14:G204	not-attacker-reachable	out, err := exec.Command("df", "-Pk", path).Output() runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the doctor, a developer-run diagnostic over the local stack
internal/devorchestrator/doctor/measure.go:244:14:G204	not-attacker-reachable	out, err := exec.Command(name, args...).Output() runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the doctor, a developer-run diagnostic over the local stack
internal/devorchestrator/doctor/measure.go:255:9:G204	not-attacker-reachable	return exec.CommandContext(ctx, name, args...).Run() == nil runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the doctor, a developer-run diagnostic over the local stack
internal/devorchestrator/doctor/measure.go:277:9:G204	not-attacker-reachable	cmd := exec.CommandContext(ctx, "bash", "-c", def.verCmd) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the doctor, a developer-run diagnostic over the local stack
internal/devorchestrator/doctor/output.go:228:2:G104	not-a-security-issue	the unchecked error at enc.Encode(out) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the doctor, a developer-run diagnostic over the local stack)
internal/devorchestrator/driver.go:180:12:G301	not-attacker-reachable	if err := os.MkdirAll(filepath.Dir(paths.Lock), 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/driver.go:185:12:G302	not-attacker-reachable	f, err := os.OpenFile(paths.Lock, os.O_CREATE|os.O_RDWR|noFollow, 0o644) opens a lock file with mode 0644 inside the rddev orchestrator's own runtime state under .rddev/; the content is a pid, not a secret, and the path is not chosen by a request
internal/devorchestrator/driver.go:192:3:G104	not-a-security-issue	the unchecked error at f.Close() is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev orchestrator's own runtime state under .rddev/)
internal/devorchestrator/driver.go:196:3:G104	not-a-security-issue	the unchecked error at f.Close() is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev orchestrator's own runtime state under .rddev/)
internal/devorchestrator/driver.go:200:3:G104	not-a-security-issue	the unchecked error at f.Close() is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev orchestrator's own runtime state under .rddev/)
internal/devorchestrator/driver_run.go:51:9:G204	not-attacker-reachable	cmd := exec.Command(o.binary(), o.rddevArgs(args)...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the rddev orchestrator's own runtime state under .rddev/
internal/devorchestrator/gate_inputs.go:122:12:G301	not-attacker-reachable	if err := os.MkdirAll(dir, 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/gate_inputs.go:142:16:G304	not-attacker-reachable	data, err := os.ReadFile(src): the map it ranges over was built by this function from the gate's own input list, and both ends are paths inside the gate's run directory
internal/devorchestrator/gate_inputs.go:147:13:G301	not-attacker-reachable	if err := os.MkdirAll(filepath.Dir(dst), 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/gate_inputs.go:285:15:G304	not-attacker-reachable	auth, err := os.ReadFile(filepath.Join(RuntimeTasksDir(repoRoot, taskID), rel)) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/gate_inputs.go:289:15:G304	not-attacker-reachable	work, err := os.ReadFile(filepath.Join(resultDir, rel)) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/gate_inputs.go:332:12:G301	not-attacker-reachable	if err := os.MkdirAll(RuntimeTasksDir(repoRoot, taskID), 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/gate_inputs.go:348:12:G301	not-attacker-reachable	if err := os.MkdirAll(WorkerTaskDir(repoRoot, taskID), 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/gate_records.go:47:12:G301	not-attacker-reachable	if err := os.MkdirAll(GatesDir(repoRoot, taskID), 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/gate_records.go:212:16:G304	not-attacker-reachable	data, err := os.ReadFile(filepath.Join(GatesDir(repoRoot, taskID), name)) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/gate_records.go:240:15:G304	not-attacker-reachable	data, err := os.ReadFile(filepath.Join(GatesDir(repoRoot, taskID), best)) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/gate_run.go:331:12:G301	not-attacker-reachable	if err := os.MkdirAll(outDir, 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/gate_run.go:335:15:G304	not-attacker-reachable	logf, err := os.Create(outPath) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/gate_run.go:340:9:G204	not-attacker-reachable	cmd := exec.Command("bash", "--noprofile", "--norc", "-eo", "pipefail", "-c", step.Run) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the rddev orchestrator's own runtime state under .rddev/
internal/devorchestrator/gate_run.go:384:2:G104	not-a-security-issue	the unchecked error at logf.Close() is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev orchestrator's own runtime state under .rddev/)
internal/devorchestrator/gate_run.go:781:3:G104	not-a-security-issue	the unchecked error at patchFile.Close() is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev orchestrator's own runtime state under .rddev/)
internal/devorchestrator/gate_run.go:782:3:G104	not-a-security-issue	the unchecked error at os.Remove(patch) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev orchestrator's own runtime state under .rddev/)
internal/devorchestrator/gate_run.go:787:3:G104	not-a-security-issue	the unchecked error at os.Remove(patch) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev orchestrator's own runtime state under .rddev/)
internal/devorchestrator/gate_spec.go:90:15:G304	not-attacker-reachable	data, err := os.ReadFile(path) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/git_control.go:206:9:G204	not-attacker-reachable	cmd := exec.Command(gh, args...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the rddev orchestrator's own runtime state under .rddev/
internal/devorchestrator/git_control.go:240:9:G204	not-attacker-reachable	cmd := exec.Command(gh, args...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the rddev orchestrator's own runtime state under .rddev/
internal/devorchestrator/git_control.go:726:16:G304	not-attacker-reachable	data, err := os.ReadFile(abs) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/git_control.go:861:17:G304	not-attacker-reachable	data, err := os.ReadFile(filepath.Join(dirReal, filepath.Base(abs))) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/git_control.go:1011:2:G104	not-a-security-issue	the unchecked error at f.Close() is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev orchestrator's own runtime state under .rddev/)
internal/devorchestrator/migration_numbers.go:112:12:G301	not-attacker-reachable	if err := os.MkdirAll(filepath.Dir(path), 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/rebaseline.go:42:12:G301	not-attacker-reachable	if err := os.MkdirAll(parent, 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/rebaseline.go:51:10:G301	not-attacker-reachable	err := os.Mkdir(dir, 0o755) creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/rebaseline.go:72:9:G204	not-attacker-reachable	cmd := exec.Command("git", args...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the rddev orchestrator's own runtime state under .rddev/
internal/devorchestrator/rebaseline.go:363:4:G104	not-a-security-issue	the unchecked error at os.RemoveAll(keep) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev orchestrator's own runtime state under .rddev/)
internal/devorchestrator/rebaseline.go:377:4:G104	not-a-security-issue	the unchecked error at os.RemoveAll(keep) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev orchestrator's own runtime state under .rddev/)
internal/devorchestrator/rebaseline.go:478:9:G204	not-attacker-reachable	c := exec.Command(cmd[0], cmd[1:]...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the rddev orchestrator's own runtime state under .rddev/
internal/devorchestrator/rebaseline.go:528:8:G204	not-attacker-reachable	c := exec.Command(cmd[0], cmd[1:]...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the rddev orchestrator's own runtime state under .rddev/
internal/devorchestrator/rebaseline.go:653:12:G301	not-attacker-reachable	if err := os.MkdirAll(files, 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/rebaseline.go:754:12:G301	not-attacker-reachable	if err := os.MkdirAll(filepath.Dir(dst), 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/rebaseline.go:772:16:G304	not-attacker-reachable	data, err := os.ReadFile(src) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/rebaseline.go:776:25:G703	not-attacker-reachable	if err := os.WriteFile(dst, data, 0o600); err != nil { reads or writes a path taken from the rddev orchestrator's own runtime state under .rddev/; the taint source the rule needs is an attacker who can already choose that path (see the notes on the individual sites)
internal/devorchestrator/rebaseline.go:798:13:G301	not-attacker-reachable	if err := os.MkdirAll(dst, 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/rebaseline.go:1035:9:G204	not-attacker-reachable	cmd := exec.Command("git", args...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the rddev orchestrator's own runtime state under .rddev/
internal/devorchestrator/rebaseline.go:1170:9:G204	not-attacker-reachable	cmd := exec.Command("git", "ls-tree", "-r", "-z", rev) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the rddev orchestrator's own runtime state under .rddev/
internal/devorchestrator/rebaseline.go:1434:12:G301	not-attacker-reachable	if err := os.MkdirAll(filepath.Dir(dst), 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/rebaseline.go:1462:13:G301	not-attacker-reachable	if err := os.MkdirAll(dst, 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/rebaseline.go:1484:18:G304	not-attacker-reachable	target, err := os.ReadFile(filepath.Join(keep, "files", filepath.FromSlash(e.Path))) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/rebaseline.go:1495:16:G304	not-attacker-reachable	data, err := os.ReadFile(filepath.Join(keep, "files", filepath.FromSlash(e.Path))): e.Path comes from the snapshot index this same tool wrote, and git refuses a tree entry whose path escapes its root, so the join cannot leave the snapshot directory
internal/devorchestrator/rebaseline.go:1499:25:G703	not-attacker-reachable	if err := os.WriteFile(dst, data, e.Mode); err != nil {: dst is the restore target under the snapshot's keep directory, with e.Path from the index this tool wrote (git tree entries cannot contain a .. component)
internal/devorchestrator/rebaseline.go:1560:16:G304	not-attacker-reachable	data, err := os.ReadFile(root) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/rebaseline.go:1574:27:G122	not-attacker-reachable	data, err := os.ReadFile(path) runs inside a Walk callback over the rddev orchestrator's own runtime state under .rddev/; the symlink race the rule describes needs an attacker who can already write into that directory, which is the same trust domain as the process doing the walk
internal/devorchestrator/rebaseline.go:1574:16:G304	not-attacker-reachable	data, err := os.ReadFile(path) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/rebaseline.go:1684:16:G304	not-attacker-reachable	want, err := os.ReadFile(filepath.Join(keep, "files", filepath.FromSlash(e.Path))) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/rebaseline.go:1740:16:G304	not-attacker-reachable	data, err := os.ReadFile(abs) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/rebaseline.go:1846:25:G703	not-attacker-reachable	if err := os.WriteFile(p, data, 0o600); err != nil { reads or writes a path taken from the rddev orchestrator's own runtime state under .rddev/; the taint source the rule needs is an attacker who can already choose that path (see the notes on the individual sites)
internal/devorchestrator/rebaseline.go:1851:14:G204	not-attacker-reachable	out, err := exec.Command("git", "merge-file", "-p", paths[0], paths[1], paths[2]).Output() runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the rddev orchestrator's own runtime state under .rddev/
internal/devorchestrator/rebaseline.go:1872:9:G204	not-attacker-reachable	cmd := exec.Command("git", args...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the rddev orchestrator's own runtime state under .rddev/
internal/devorchestrator/ref_ledger.go:125:15:G304	not-attacker-reachable	data, err := os.ReadFile(path) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/ref_ledger.go:151:12:G301	not-attacker-reachable	if err := os.MkdirAll(filepath.Dir(path), 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/result_consistency.go:57:14:G304	not-attacker-reachable	raw, err := os.ReadFile(resultPath) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/result_freshness.go:87:15:G304	not-attacker-reachable	data, err := os.ReadFile(filepath.Join(resultDir, "RESULT.json")) takes the dispatch-time fingerprint of a task's result dir: that dir is rddev's own runtime state under .rddev/workers/<TASK>/, assembled by WorkerTaskDir from the task id rddev was invoked with, and no HTTP request or other untrusted input reaches the name — the finding is the same shape as the result-dir reads already reviewed in gate_inputs.go and worker_collect.go
internal/devorchestrator/result_freshness.go:123:15:G304	not-attacker-reachable	data, err := os.ReadFile(resultPath) reads the RESULT.json that this same collect run is judging: resultPath is rec.ResultDir + "RESULT.json", i.e. rddev's own registry entry under .rddev/workers/<TASK>/, not a name any HTTP request or other untrusted input decides — the finding is the same shape as the result-dir reads already reviewed in gate_inputs.go and worker_collect.go
internal/devorchestrator/review_worker.go:128:12:G301	not-attacker-reachable	if err := os.MkdirAll(scratch, 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/review_worker.go:131:12:G301	not-attacker-reachable	if err := os.MkdirAll(reviewDir, 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/review_worker.go:170:13:G306	not-attacker-reachable	if err := os.WriteFile(filepath.Join(reviewDir, name), []byte(content), 0o644); err != nil { writes with mode 0644/0755 inside the rddev orchestrator's own runtime state under .rddev/; the content is a generated artifact or a report, and the path is not chosen by a request
internal/devorchestrator/review_worker.go:196:12:G306	not-attacker-reachable	if err := os.WriteFile(filepath.Join(reviewDir, "prompt.md"), []byte(prompt), 0o644); err != nil { writes with mode 0644/0755 inside the rddev orchestrator's own runtime state under .rddev/; the content is a generated artifact or a report, and the path is not chosen by a request
internal/devorchestrator/review_worker.go:200:12:G306	not-attacker-reachable	if err := os.WriteFile(systemPath, []byte(system), 0o644); err != nil { writes with mode 0644/0755 inside the rddev orchestrator's own runtime state under .rddev/; the content is a generated artifact or a report, and the path is not chosen by a request
internal/devorchestrator/review_worker.go:266:9:G204	not-attacker-reachable	cmd := exec.Command("bash", cmdArgs...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the rddev orchestrator's own runtime state under .rddev/
internal/devorchestrator/review_worker.go:483:15:G306	not-attacker-reachable	if werr := os.WriteFile(verdictPath, recovered, 0o644); werr != nil { writes with mode 0644/0755 inside the rddev orchestrator's own runtime state under .rddev/; the content is a generated artifact or a report, and the path is not chosen by a request
internal/devorchestrator/review_worker.go:494:15:G304	not-attacker-reachable	data, err := os.ReadFile(verdictPath) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/review_worker.go:605:19:G304	not-attacker-reachable	if data, err := os.ReadFile(filepath.Join(rec.Worktree, p)); err == nil { reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/review_worker.go:781:12:G304	not-attacker-reachable	f, err := os.Open(logPath) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/scope_validate.go:44:15:G304	not-attacker-reachable	data, err := os.ReadFile(path) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/store.go:491:13:G301	not-attacker-reachable	if err := os.MkdirAll(dir, 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/store.go:495:12:G302	not-attacker-reachable	f, err := os.OpenFile(s.lockPath, os.O_CREATE|os.O_RDWR, 0o644) opens a lock file with mode 0644 inside the rddev orchestrator's own runtime state under .rddev/; the content is a pid, not a secret, and the path is not chosen by a request
internal/devorchestrator/store.go:500:3:G104	not-a-security-issue	the unchecked error at f.Close() is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev orchestrator's own runtime state under .rddev/)
internal/devorchestrator/store.go:504:3:G104	not-a-security-issue	the unchecked error at syscall.Flock(int(f.Fd()), syscall.LOCK_UN) // best effort is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev orchestrator's own runtime state under .rddev/)
internal/devorchestrator/store.go:505:3:G104	not-a-security-issue	the unchecked error at f.Close() is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev orchestrator's own runtime state under .rddev/)
internal/devorchestrator/store.go:582:13:G301	not-attacker-reachable	if err := os.MkdirAll(dir, 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/store.go:593:3:G104	not-a-security-issue	the unchecked error at tmp.Close() is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev orchestrator's own runtime state under .rddev/)
internal/devorchestrator/store.go:597:3:G104	not-a-security-issue	the unchecked error at tmp.Close() is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev orchestrator's own runtime state under .rddev/)
internal/devorchestrator/worker_collect.go:325:18:G304	not-attacker-reachable	if data, err := os.ReadFile(resultPath); err == nil { reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/worker_collect.go:366:17:G304	not-attacker-reachable	if raw, err := os.ReadFile(resultPath); err == nil { reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/worker_collect.go:554:15:G304	not-attacker-reachable	data, err := os.ReadFile(filepath.Join(taskDir, "task-package.json")) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/worker_collect.go:579:16:G304	not-attacker-reachable	data, err := os.ReadFile(path): targets is the collect stage's own list of files to read back (RESULT.json, the diffs it just produced), all under the worker's directory
internal/devorchestrator/worker_env.go:55:12:G301	not-attacker-reachable	if err := os.MkdirAll(ghDir, 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/worker_guard.go:195:12:G301	not-attacker-reachable	if err := os.MkdirAll(guardDir, 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/worker_guard.go:199:12:G306	not-attacker-reachable	if err := os.WriteFile(guardPath, []byte(guardScript), 0o755); err != nil { writes with mode 0644/0755 inside the rddev orchestrator's own runtime state under .rddev/; the content is a generated artifact or a report, and the path is not chosen by a request
internal/devorchestrator/worker_guard.go:206:12:G306	not-attacker-reachable	if err := os.WriteFile(filepath.Join(dir, "worker-settings.json"), settings, 0o644); err != nil { writes with mode 0644/0755 inside the rddev orchestrator's own runtime state under .rddev/; the content is a generated artifact or a report, and the path is not chosen by a request
internal/devorchestrator/worker_guard.go:209:12:G306	not-attacker-reachable	if err := os.WriteFile(filepath.Join(dir, "run-worker.sh"), []byte(reaperScript), 0o755); err != nil writes with mode 0644/0755 inside the rddev orchestrator's own runtime state under .rddev/; the content is a generated artifact or a report, and the path is not chosen by a request
internal/devorchestrator/worker_guard.go:304:15:G304	not-attacker-reachable	data, err := os.ReadFile(schemaPath) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/worker_proc.go:333:16:G304	not-attacker-reachable	data, err := os.ReadFile(f) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/worker_registry.go:173:13:G301	not-attacker-reachable	if err := os.MkdirAll(dir, 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/worker_registry.go:177:12:G302	not-attacker-reachable	f, err := os.OpenFile(path, os.O_CREATE|os.O_RDWR, 0o644) opens a lock file with mode 0644 inside the rddev orchestrator's own runtime state under .rddev/; the content is a pid, not a secret, and the path is not chosen by a request
internal/devorchestrator/worker_registry.go:177:12:G304	not-attacker-reachable	f, err := os.OpenFile(path, os.O_CREATE|os.O_RDWR, 0o644) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/worker_registry.go:182:3:G104	not-a-security-issue	the unchecked error at f.Close() is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev orchestrator's own runtime state under .rddev/)
internal/devorchestrator/worker_registry.go:186:3:G104	not-a-security-issue	the unchecked error at syscall.Flock(int(f.Fd()), syscall.LOCK_UN) // best effort is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev orchestrator's own runtime state under .rddev/)
internal/devorchestrator/worker_registry.go:187:3:G104	not-a-security-issue	the unchecked error at f.Close() is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the rddev orchestrator's own runtime state under .rddev/)
internal/devorchestrator/worker_render.go:94:15:G304	not-attacker-reachable	data, err := os.ReadFile(schemaPath) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/worker_render.go:338:21:G304	not-attacker-reachable	schemaData, err := os.ReadFile(schemaPath) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/worker_render.go:346:21:G304	not-attacker-reachable	resultData, err := os.ReadFile(resultPath) reads a path built from the rddev orchestrator's own runtime state under .rddev/ — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
internal/devorchestrator/worker_spawn.go:360:9:G204	not-attacker-reachable	cmd := exec.Command("bash", cmdArgs...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the rddev orchestrator's own runtime state under .rddev/
internal/devorchestrator/worker_spawn.go:767:12:G301	not-attacker-reachable	if err := os.MkdirAll(WorkersDir(repoRoot), 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/worker_spawn.go:790:12:G301	not-attacker-reachable	if err := os.MkdirAll(taskDir, 0o755); err != nil { creates a directory with mode 0755 inside the rddev orchestrator's own runtime state under .rddev/ — the mode is the ordinary directory mode, and the path is not chosen by a request
internal/devorchestrator/worker_spawn.go:850:9:G204	not-attacker-reachable	cmd := exec.CommandContext(ctx, "git", args...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the rddev orchestrator's own runtime state under .rddev/
internal/devorchestrator/worker_spawn.go:880:9:G204	not-attacker-reachable	cmd := exec.Command("git", args...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the rddev orchestrator's own runtime state under .rddev/
internal/devorchestrator/worker_spawn.go:899:9:G204	not-attacker-reachable	cmd := exec.Command("git", args...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the rddev orchestrator's own runtime state under .rddev/
internal/devorchestrator/worker_spawn.go:914:9:G204	not-attacker-reachable	cmd := exec.Command(bin, args...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the rddev orchestrator's own runtime state under .rddev/
internal/gitprovider/config.go:95:2:G101	not-a-security-issue	EnvToken = "POST_GITEA_TOKEN": the value is the NAME of an environment variable (POST_GITEA_TOKEN) that the adapter reads at startup; the secret itself is never in the source
internal/gitprovider/config.go:98:2:G101	not-a-security-issue	EnvAdminPassword = "POST_GITEA_ADMIN_PASSWORD": the value is the NAME of an environment variable (POST_GITEA_ADMIN_PASSWORD) that the adapter reads at startup; the secret itself is never in the source
internal/gitprovider/gitea.go:68:9:G204	not-attacker-reachable	cmd := exec.CommandContext(ctx, "git", args...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the git provider adapter, whose input is the internal Gitea instance
internal/gitprovider/gitea.go:68:28:G702	not-a-security-issue	cmd := exec.CommandContext(ctx, "git", args...): the rule's claim is command injection, and there is no shell here — the binary is the constant "git" and the arguments are passed as argv from the git provider adapter, whose input is the internal Gitea instance
internal/observability/serve.go:52:2:G118	not-a-security-issue	go func() { starts a goroutine that waits on the ctx it was handed and is not a request-scoped continuation
internal/persistence/audit_store.go:96:27:G115	bounded	PageLimit: int32(limit),: limit is clamped by audit.Service.page (limit <= 0 becomes DefaultLimit, limit > maxLimit becomes maxLimit) before it reaches the store
internal/persistence/audit_store.go:138:24:G115	bounded	PageLimit: int32(limit),: limit is clamped by audit.Service.page (limit <= 0 becomes DefaultLimit, limit > maxLimit becomes maxLimit) before it reaches the store
internal/persistence/feed_store.go:86:19:G115	bounded	RowLimit: int32(limit),: limit is the feed service's own configured cap (feeds.Service.max, defaulting to DefaultMaxEntries = 50), never a request value
internal/persistence/feed_store.go:133:18:G115	bounded	RowLimit: int32(limit),: limit is the feed service's own configured cap (feeds.Service.max, defaulting to DefaultMaxEntries = 50), never a request value
internal/persistence/feed_store.go:175:18:G115	bounded	RowLimit: int32(limit),: limit is the feed service's own configured cap (feeds.Service.max, defaulting to DefaultMaxEntries = 50), never a request value
internal/persistence/memstore/memstore.go:74:14:G115	not-a-security-issue	b[i] = byte(n * (i + 1)): the narrowing IS the function's job — a deterministic counter shaped into 16 bytes for a test-only identity space; the value never leaves a test process
internal/persistence/relation_store.go:125:28:G115	bounded	ExpectedVersionNo: int32(expected),: expected is the relation's own current_version_no from an int4 column
internal/persistence/relation_store.go:213:35:G115	bounded	RelationID: id, VersionNo: int32(versionNo),: the version comes from the route parameter after the same MaxInt32 check the object path uses, and relation_version.version_no is int4
internal/persistence/relation_store.go:416:22:G115	bounded	VersionNo: int32(versionNo),: versionNo is 0 or the relation's own current_version_no from an int4 column
internal/persistence/relation_tx.go:95:27:G115	bounded	ExpectedVersionNo: int32(expected),: expected is the relation's own current_version_no from an int4 column
internal/persistence/scientific_object_abort.go:52:27:G115	bounded	ExpectedVersionNo: int32(in.ExpectedVersionNo),: in.ExpectedVersionNo is the object's own current_version_no from an int4 column, not a client-supplied number
internal/persistence/scientific_object_reopen.go:51:27:G115	bounded	ExpectedVersionNo: int32(in.ExpectedVersionNo),: in.ExpectedVersionNo is the object's own current_version_no from an int4 column, not a client-supplied number
internal/persistence/scientific_object_store.go:129:28:G115	bounded	ExpectedVersionNo: int32(expected),: expected is the object row's own current_version_no (aborts/service.go:537, reopens/service.go:564 pass obj.CurrentVersionNo), an int4 column value
internal/persistence/scientific_object_store.go:305:24:G115	bounded	VersionNo: int32(versionNo),: versionNo is 0, or the object's own current_version_no read back from an int4 column — GetVersion refuses a version above math.MaxInt32 explicitly (scientific_object_store.go:214), so the conversion cannot truncate
internal/persistence/scientific_object_tx.go:94:27:G115	bounded	ExpectedVersionNo: int32(expected),: expected is the object's own current_version_no from an int4 column
internal/persistence/sqlc/research_profile.sql.go:321:7:G101	not-a-security-issue	const listOrganizationAssetCredits = `-- name: ListOrganizationAssetCredits :many: the same, for ListOrganizationAssetCredits — a generated SQL statement constant, not a credential
internal/persistence/sqlc/research_profile.sql.go:558:7:G101	not-a-security-issue	const listPersonAssetCredits = `-- name: ListPersonAssetCredits :many: the match is the generated sqlc query constant — a SQL statement, not a credential; gosec's entropy check matched a word inside the query text
internal/persistence/testdb/testdb.go:113:7:G306	not-attacker-reachable	_ = os.WriteFile(path, []byte(report), 0o644) writes with mode 0644/0755 inside the test-database helper, whose input is $POST_TESTDB_STATS; the content is a generated artifact or a report, and the path is not chosen by a request
internal/persistence/testdb/testdb.go:113:19:G703	not-attacker-reachable	_ = os.WriteFile(path, []byte(report), 0o644) reads or writes a path taken from the test-database helper, whose input is $POST_TESTDB_STATS; the taint source the rule needs is an attacker who can already choose that path (see the notes on the individual sites)
internal/search/embedding/batch.go:165:27:G115	not-attacker-reachable	BatchSize: int32(w.batchSize),: w.batchSize is set by the worker's own option function (DefaultBatchSize) and only ever checked positive at batch.go:119 — no request supplies it, so the rule's attacker-controlled-input premise does not hold. It is not bounded by a validation the way the page limits are; it is simply not reachable from outside the process.
internal/search/retrieval/store.go:51:27:G115	bounded	PageSize: int32(q.PageSize),: q.PageSize is checked against Limits.PerSignal before this line (retrieval.go:133 refuses per_signal outside 1..MaxPerSignal, MaxPerSignal = 200)
internal/search/retrieval/store.go:85:27:G115	bounded	PageSize: int32(q.PageSize),: q.PageSize is checked against Limits.PerSignal before this line (retrieval.go:133 refuses per_signal outside 1..MaxPerSignal, MaxPerSignal = 200)
internal/search/retrieval/store.go:115:27:G115	bounded	PageSize: int32(q.PageSize),: q.PageSize is checked against Limits.PerSignal before this line (retrieval.go:133 refuses per_signal outside 1..MaxPerSignal, MaxPerSignal = 200)
tests/acceptance/portability/bundle.go:246:12:G304	not-attacker-reachable	f, err := os.Open(path) reads a path built from the export/portability harness, whose input is the bundle directory it was handed — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
tests/acceptance/portability/bundle.go:261:14:G304	not-attacker-reachable	raw, err := os.ReadFile(filepath.Join(bundleDir, indexFile)) reads a path built from the export/portability harness, whose input is the bundle directory it was handed — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
tests/acceptance/portability/bundle.go:302:15:G304	not-attacker-reachable	mraw, err := os.ReadFile(filepath.Join(bundleDir, manifestF)) reads a path built from the export/portability harness, whose input is the bundle directory it was handed — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
tests/acceptance/portability/bundle.go:323:15:G304	not-attacker-reachable	rraw, err := os.ReadFile(filepath.Join(bundleDir, releaseF)) reads a path built from the export/portability harness, whose input is the bundle directory it was handed — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
tests/acceptance/portability/bundle.go:427:12:G301	not-attacker-reachable	if err := os.MkdirAll(filepath.Dir(full), 0o755); err != nil { creates a directory with mode 0755 inside the export/portability harness, whose input is the bundle directory it was handed — the mode is the ordinary directory mode, and the path is not chosen by a request
tests/acceptance/portability/bundle.go:430:12:G306	not-attacker-reachable	if err := os.WriteFile(full, body, 0o644); err != nil { writes with mode 0644/0755 inside the export/portability harness, whose input is the bundle directory it was handed; the content is a generated artifact or a report, and the path is not chosen by a request
tests/acceptance/portability/bundle.go:483:11:G301	not-attacker-reachable	return os.MkdirAll(target, 0o755) creates a directory with mode 0755 inside the export/portability harness, whose input is the bundle directory it was handed — the mode is the ordinary directory mode, and the path is not chosen by a request
tests/acceptance/portability/bundle.go:485:27:G122	not-attacker-reachable	body, err := os.ReadFile(p) runs inside a Walk callback over the export/portability harness, whose input is the bundle directory it was handed; the symlink race the rule describes needs an attacker who can already write into that directory, which is the same trust domain as the process doing the walk
tests/acceptance/portability/bundle.go:485:16:G304	not-attacker-reachable	body, err := os.ReadFile(p) reads a path built from the export/portability harness, whose input is the bundle directory it was handed — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
tests/acceptance/portability/bundle.go:493:22:G703	not-attacker-reachable	return os.WriteFile(target, body, info.Mode().Perm()) reads or writes a path taken from the export/portability harness, whose input is the bundle directory it was handed; the taint source the rule needs is an attacker who can already choose that path (see the notes on the individual sites)
tests/acceptance/portability/bundle.go:510:9:G306	not-attacker-reachable	return os.WriteFile(filepath.Join(bundleDir, indexFile), append(raw, '\n'), 0o644) writes with mode 0644/0755 inside the export/portability harness, whose input is the bundle directory it was handed; the content is a generated artifact or a report, and the path is not chosen by a request
tests/acceptance/portability/export.go:147:14:G304	not-attacker-reachable	raw, err := os.ReadFile(planPath) reads a path built from the export/portability harness, whose input is the bundle directory it was handed — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
tests/acceptance/portability/export.go:275:12:G301	not-attacker-reachable	if err := os.MkdirAll(outDir, 0o755); err != nil { creates a directory with mode 0755 inside the export/portability harness, whose input is the bundle directory it was handed — the mode is the ordinary directory mode, and the path is not chosen by a request
tests/acceptance/portability/git.go:54:9:G204	not-attacker-reachable	cmd := exec.Command("git", args...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the export/portability harness, whose input is the bundle directory it was handed
tests/acceptance/portability/import.go:214:14:G304	not-attacker-reachable	raw, err := os.ReadFile(filepath.Join(bundleDir, stateF)) reads a path built from the export/portability harness, whose input is the bundle directory it was handed — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
tests/acceptance/portability/import.go:269:16:G304	not-attacker-reachable	body, err := os.ReadFile(filepath.Join(bundleDir, blobsDir, b.ContentHash)) reads a path built from the export/portability harness, whose input is the bundle directory it was handed — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
tests/acceptance/portability/import.go:522:16:G304	not-attacker-reachable	body, err := os.ReadFile(target) reads a path built from the export/portability harness, whose input is the bundle directory it was handed — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
tests/acceptance/portability/import.go:530:13:G306	not-attacker-reachable	if err := os.WriteFile(target, body, 0o644); err != nil { writes with mode 0644/0755 inside the export/portability harness, whose input is the bundle directory it was handed; the content is a generated artifact or a report, and the path is not chosen by a request
tests/acceptance/portability/import.go:530:25:G703	not-attacker-reachable	if err := os.WriteFile(target, body, 0o644); err != nil { reads or writes a path taken from the export/portability harness, whose input is the bundle directory it was handed; the taint source the rule needs is an attacker who can already choose that path (see the notes on the individual sites)
tests/acceptance/portability/import.go:536:15:G304	not-attacker-reachable	raw, err := os.ReadFile(path) reads a path built from the export/portability harness, whose input is the bundle directory it was handed — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
tests/acceptance/portability/import.go:553:13:G306	not-attacker-reachable	if err := os.WriteFile(path, body, 0o644); err != nil { writes with mode 0644/0755 inside the export/portability harness, whose input is the bundle directory it was handed; the content is a generated artifact or a report, and the path is not chosen by a request
tests/acceptance/portability/main.go:186:10:G306	not-attacker-reachable	return os.WriteFile(*out, append(body, '\n'), 0o644) writes with mode 0644/0755 inside the export/portability harness, whose input is the bundle directory it was handed; the content is a generated artifact or a report, and the path is not chosen by a request
tests/acceptance/seeddemo/main.go:85:15:G306	not-attacker-reachable	if werr := os.WriteFile(*report, append(raw, '\n'), 0o644); werr != nil { writes with mode 0644/0755 inside the seed-demo harness, whose input is the plan file it was handed; the content is a generated artifact or a report, and the path is not chosen by a request
tests/acceptance/seeddemo/main.go:167:14:G306	not-attacker-reachable	if werr := os.WriteFile(*jsonOut, append(raw, '\n'), 0o644); werr != nil { writes with mode 0644/0755 inside the seed-demo harness, whose input is the plan file it was handed; the content is a generated artifact or a report, and the path is not chosen by a request
tests/acceptance/seeddemo/plan.go:227:14:G304	not-attacker-reachable	raw, err := os.ReadFile(path) reads a path built from the seed-demo harness, whose input is the plan file it was handed — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
tests/benchmark/db.go:26:7:G101	not-a-security-issue	const defaultAdminURL = "postgres://postgres:postgres_dev_pw@127.0.0.1:5432/post": same local default as above (a developer's docker-compose Postgres on 127.0.0.1), overridable via POSTGRES_TEST_ADMIN_URL, with no reach beyond the machine
tests/benchmark/db.go:155:49:G115	bounded	buf = binary.BigEndian.AppendUint16(buf, uint16(len(e.vec))): the value converted here is bounded before it reaches this line (see the notes on the individual sites)
tests/benchmark/seed.go:235:9:G404	not-a-security-issue	rng := rand.New(rand.NewPCG(0x7108, 0xBE0C)) seeds a deterministic PRNG so the benchmark corpus is reproducible; nothing security-relevant is derived from it
tests/benchmark/selfcheck.go:156:9:G101	not-a-security-issue	const secret = "s3cr3t-not-a-credential": the variable is the redaction self-check's own fixture, named so that no reader can mistake it for a real secret, and it is only ever passed to redactURL
tests/benchmark/selfcheck.go:158:3:G101	not-a-security-issue	want := "postgres://postgres:xxxxx@127.0.0.1:5432/post_bench" is the expected OUTPUT of the redaction self-check (the credential must be gone from it) — matching the literal here means redaction failed, which is the opposite of a leaked credential
tests/benchmark/workload.go:503:23:G115	bounded	PageOffset: int32(c.Scale.NetworkDocuments) - 20,: the value converted here is bounded before it reaches this line (see the notes on the individual sites)
tests/contract/contract.go:51:14:G304	not-attacker-reachable	raw, err := os.ReadFile(path) reads a path built from the contract-test harness, whose input is the path on its command line — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
tests/contract/decision.go:75:14:G304	not-attacker-reachable	raw, err := os.ReadFile(abs) reads a path built from the contract-test harness, whose input is the path on its command line — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
tests/contract/decision.go:109:3:G104	not-a-security-issue	the unchecked error at fmt.Sscanf(m[1], "%d", &n) is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the contract-test harness, whose input is the path on its command line)
tests/contract/exemptions.go:44:14:G304	not-attacker-reachable	raw, err := os.ReadFile(path) reads a path built from the contract-test harness, whose input is the path on its command line — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
tests/contract/inventory.go:185:14:G304	not-attacker-reachable	raw, err := os.ReadFile(path) reads a path built from the contract-test harness, whose input is the path on its command line — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
tests/contract/inventory.go:201:12:G301	not-attacker-reachable	if err := os.MkdirAll(filepath.Dir(path), 0o755); err != nil { creates a directory with mode 0755 inside the contract-test harness, whose input is the path on its command line — the mode is the ordinary directory mode, and the path is not chosen by a request
tests/contract/inventory.go:208:9:G306	not-attacker-reachable	return os.WriteFile(path, append(raw, '\n'), 0o644) writes with mode 0644/0755 inside the contract-test harness, whose input is the path on its command line; the content is a generated artifact or a report, and the path is not chosen by a request
tests/contract/inventory.go:301:12:G301	not-attacker-reachable	if err := os.MkdirAll(filepath.Dir(path), 0o755); err != nil { creates a directory with mode 0755 inside the contract-test harness, whose input is the path on its command line — the mode is the ordinary directory mode, and the path is not chosen by a request
tests/contract/inventory.go:308:9:G306	not-attacker-reachable	return os.WriteFile(path, append(raw, '\n'), 0o644) writes with mode 0644/0755 inside the contract-test harness, whose input is the path on its command line; the content is a generated artifact or a report, and the path is not chosen by a request
tests/contract/mcp.go:34:14:G304	not-attacker-reachable	raw, err := os.ReadFile(path) reads a path built from the contract-test harness, whose input is the path on its command line — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
tests/contract/mcp.go:244:16:G304	not-attacker-reachable	docText, _ := os.ReadFile(filepath.Join(root, opts.DocPath)) reads a path built from the contract-test harness, whose input is the path on its command line — no HTTP request or other untrusted input reaches the name (see the notes on the individual sites)
tests/e2e-pr-flows/harness/main.go:759:2:G104	not-a-security-issue	the unchecked error at resp.Body.Close() is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the end-to-end PR-flow harness, driven by run.sh against the local stack)
tests/e2e-pr-flows/harness/main.go:893:9:G204	not-attacker-reachable	cmd := exec.CommandContext(ctx, "git", append([]string{"-C", dir}, args...)...) runs a binary the tool itself resolved (the literal "git", or a LookPath'ed psql) with an argument slice and no shell, from the end-to-end PR-flow harness, driven by run.sh against the local stack
tests/e2e-pr-flows/harness/main.go:893:28:G702	not-a-security-issue	cmd := exec.CommandContext(ctx, "git", append([]string{"-C", dir}, args...)...): the rule's claim is command injection, and there is no shell here — the binary is the constant "git" and the arguments are passed as argv from the end-to-end PR-flow harness, driven by run.sh against the local stack
tests/e2e-pr-flows/harness/main.go:911:3:G104	not-a-security-issue	the unchecked error at resp.Body.Close() is a Close/Remove/Sync/io.Copy-to-Discard or a report write whose failure cannot change any security decision here (the path it would affect is the end-to-end PR-flow harness, driven by run.sh against the local stack)
