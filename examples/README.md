# 示例契约

这些 JSON 不是生产数据，而是给 Claude Code 做 API/schema/golden test 时使用的可读示例。Demo 内容必须始终标注 synthetic。

`seed-demo/` 是 seed demo project 的声明文件与说明（内容全部为 synthetic）：见 [seed-demo/README.md](seed-demo/README.md)。
