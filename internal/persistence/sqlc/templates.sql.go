// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: templates.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getTemplateInstantiationByProject = `-- name: GetTemplateInstantiationByProject :one
SELECT id, project_id, template_id, template_version, template_name, created_by, created_at FROM project_template_instantiations
WHERE project_id = $1
`

// The project's template origin — at most one row exists (UNIQUE
// (project_id)); the query answers no-row when the project was created
// without a template.
func (q *Queries) GetTemplateInstantiationByProject(ctx context.Context, projectID pgtype.UUID) (ProjectTemplateInstantiation, error) {
	row := q.db.QueryRow(ctx, getTemplateInstantiationByProject, projectID)
	var i ProjectTemplateInstantiation
	err := row.Scan(
		&i.ID,
		&i.ProjectID,
		&i.TemplateID,
		&i.TemplateVersion,
		&i.TemplateName,
		&i.CreatedBy,
		&i.CreatedAt,
	)
	return i, err
}

const insertTemplateInstantiation = `-- name: InsertTemplateInstantiation :one

INSERT INTO project_template_instantiations
    (project_id, template_id, template_version, template_name, created_by)
VALUES
    ($1, $2, $3, $4, $5)
RETURNING id, project_id, template_id, template_version, template_name, created_by, created_at
`

type InsertTemplateInstantiationParams struct {
	ProjectID       pgtype.UUID `json:"project_id"`
	TemplateID      string      `json:"template_id"`
	TemplateVersion string      `json:"template_version"`
	TemplateName    string      `json:"template_name"`
	CreatedBy       pgtype.UUID `json:"created_by"`
}

// Project template instantiations (canonical table
// project_template_instantiations; 00056). The table is append-only (the
// 00056 trigger): the provenance fact "project X was created from template
// Y version Z" never changes and never disappears. These queries INSERT
// and SELECT only.
func (q *Queries) InsertTemplateInstantiation(ctx context.Context, arg InsertTemplateInstantiationParams) (ProjectTemplateInstantiation, error) {
	row := q.db.QueryRow(ctx, insertTemplateInstantiation,
		arg.ProjectID,
		arg.TemplateID,
		arg.TemplateVersion,
		arg.TemplateName,
		arg.CreatedBy,
	)
	var i ProjectTemplateInstantiation
	err := row.Scan(
		&i.ID,
		&i.ProjectID,
		&i.TemplateID,
		&i.TemplateVersion,
		&i.TemplateName,
		&i.CreatedBy,
		&i.CreatedAt,
	)
	return i, err
}
