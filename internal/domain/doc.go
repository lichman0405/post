// Package domain holds the POST domain model: projects, research objects,
// relations, versions, evidence and the state-machine invariants in
// docs/03_GLOSSARY_DOMAIN_MODEL.md and ADR-002 (RSG as canonical state).
// It must not import concrete infrastructure (docs/52: domain depends on
// nothing concrete). T0002 scaffold — types arrive with the domain tasks.
package domain
