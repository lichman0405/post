openapi: 3.1.0
info: {title: fixture, version: 0.0.0}
servers:
  - url: /api/v1
paths:
  /projects/{projectId}/pull-requests/{number}:
    post:
      summary: one pull request, spelled with a bare parameter and no verb suffix
      responses: {'200': {description: OK}}
