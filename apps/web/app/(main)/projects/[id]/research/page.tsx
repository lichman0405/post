import { TabPlaceholder } from "../tab-placeholder";

/** Research tab: the Research State workspace (RSG) lands with the
 *  research milestones; the shell route is navigable today. */
export default function ResearchPage() {
  return (
    <TabPlaceholder title="Research" milestone="the research workspace milestones (T0201+)">
      Research questions, findings and the research map organize here.
    </TabPlaceholder>
  );
}
