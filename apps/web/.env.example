# Server-side service discovery for the status page (server components fetch
# these over HTTP; nothing here reaches the browser).
API_BASE_URL=http://127.0.0.1:8080
SCIENTIFIC_ADAPTER_URL=http://127.0.0.1:9100
